import Underline from "./Underline";

const Topic = function ({ text }) {
  console.log(Text);
  return (
    <h1>
      <span className="text-4xl font-semibold text-stone-800">{text}</span>
      <Underline />
    </h1>
  );
};

export default Topic;
